@inject Services.GameService GameService

<MudContainer>
    <MudButton OnClick="SubmitMove">Submit Move</MudButton>
</MudContainer>

@code {

    [Parameter] public EventCallback<Move> OnSubmitMove { get; set; }

    private async Task SubmitMove()
    {
        await OnSubmitMove.InvokeAsync();
    }
}